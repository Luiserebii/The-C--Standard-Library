#include "../lib/mint/mint.h"
#include "../../algorithm/algorithm.h"

#include <iostream>
//#include <algorithm>
#include <vector>

using lsb::equal;
using lsb::find;
using lsb::copy;

using std::vector;

void tequal(Mint& m);

int main() {

    Mint m;
    m.title("<algorithm>");

    tequal(Mint& m);

    return m.end();
}

void tequal(Mint& m) {
    vector<int> v1, v2;
    v1.push_back(1), v2.push_back(2);
    v1.push_back(2);
    v1.push_back(4);
    v1.push_back(9);
    v1.push_back(16);
}
